// ── Shared constants ─────────────────────────────────────────

export const SPEED_SCALE = 1.2;
export const MOUSE_OFFSCREEN = -9999;
export const TRANSITION_DURATION = 2000;

// ── Mouse hover growth constants ──────────────────────────────
export const GROWTH_SPEED = 0.08;
export const SHRINK_SPEED = 0.04;
export const MAX_GROWTH = 1.8;
